import InteractiveBackground from "./InteractiveBackground";

// The interactive background owns the canvas and the complete landing experience.
export default function Page() {
  return <InteractiveBackground />;
}